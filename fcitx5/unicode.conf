# Fcitx5 unicode addon: symbol/emoji search by Unicode name (e.g. "thumbs
# up", "em dash") on Super+. — mirroring the Windows Win+. emoji picker.
# Results commit at the caret like the clipboard picker. The default
# DirectUnicodeMode (Ctrl+Shift+U → type a hex codepoint) is kept.

[TriggerKey]
0=Super+period
